{
  "//": "Scoped typecheck for TEST_ONLY_ASSURANCE_02. The root tsconfig excludes tests and sets noCheck:true, so it proves nothing about these files. Only the runtime scripts are included: the *.test.ts suite needs Bun's type declarations, which are NOT installed, so it is deliberately excluded here and is exercised by `bun test` instead.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM"],
    "types": ["node"],
    "noCheck": false,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "exactOptionalPropertyTypes": false,
    "verbatimModuleSyntax": false
  },
  "include": [
    "target-guard.ts",
    "fixtures.ts",
    "durable-sink.ts",
    "secret-vault.ts",
    "provision.ts",
    "strict-evidence.ts",
    "assurance-fetch.ts",
    "run-provision.ts",
    "run-assurance.ts",
    "run-existing-student-assurance.ts"

  ]
}
