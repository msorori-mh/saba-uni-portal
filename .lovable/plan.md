# استكمال مهمة Councils Production E2E — التوقف الحالي

## الحالة الحالية
- تم نشر الـ SHA الحالي (`f8c69ff8`) وتحقق من تطابقه مع النشر.
- رمزي مسجل ويظهر له دوره المزدوج: رئيس مجلس قسم نظم المعلومات + عضو مجلس الكلية.
- تم جدولة اجتماع إنتاجي `2203e5d4` لمجلس القسم وفتح استقبال الموضوعات.
- تم تقديم موضوع اختباري `SR-5c421a97` ووصل لقائمة المراجعة ونُقل إلى `under_review`.
- **نقطة التوقف**: زر «قبول للجدول» يظهر لرمزي لكن الموضوع ما زال `under_review` في قاعدة الإنتاج.

## الأهداف
1. تشخيص وإصلاح سبب عدم انتقال الموضوع إلى `accepted_for_agenda`.
2. إكمال رحلة رمزي الكاملة في مجلس القسم.
3. تنفيذ رحلات مقبل (رئيس مجلس الكلية) ويحيى (عضو) وغسان.
4. تنظيف بيانات الاختبار وتحديث release stamp.

## الخطوات التفصيلية

### 1. تشخيص وإصلاح عطل «قبول للجدول»
- قراءة سجلات runtime errors / server-function logs للبحث عن `council_review_topic` أو `can_review_council_topic_final`.
- التحقق من حالة الموضوع `SR-5c421a97` في `academic_council_topics` وعضوية رمزي في `academic_council_members`.
- فحص استجابة `reviewCouncilTopic` في المتصفح (network + toast) لمعرفة إن كانت تنجح ظاهرياً دون تحديث الحالة.
- إذا كان الخلل في RPC: مراجعة `council_review_topic` و `can_review_council_topic_final` وإصلاح forward-only عبر migration.
- إذا كان الخلل في invalidation: إضافة invalidation صريح لـ `chair-agenda-topics` و `my-council-topics` بعد القبول.

### 2. إكمال رحلة رمزي (رئيس مجلس قسم نظم المعلومات)
- قبول الموضوع للجدول (`accepted_for_agenda`).
- فتح نافذة جدول الأعمال `CouncilAgendaDialog` وإضافة الموضوع إلى الأجندة.
- إعادة ترتيب البنود وإضافة بند يدوي اختباري.
- اعتماد الأجندة (`finalizeMeetingAgenda`).
- انتقال الاجتماع من `agenda_finalized` إلى `in_session` عبر `transitionMyCouncilMeeting`.
- تسجيل الحضور والنصاب.
- التصويت على الموضوع واعتماد المحضر.
- التحقق من ظهور النتيجة في سجل الاجتماع.

### 3. رحلة مقبل (رئيس مجلس الكلية)
- تسجيل الدخول بحساب `maqbol@usr.edu.ye`.
- جدولة اجتماع مجلس الكلية.
- فتح استقبال الموضوعات.
- تقديم/قبول موضوع يعرض من مجلس قسم نظم المعلومات.
- بناء الأجندة والاعتماد والجلسة والتصويت.

### 4. رحلة يحيى (عضو عادي)
- تسجيل الدخول بحساب `yahya@usr.edu.ye`.
- التحقق من ظهور الاجتماعات والموضوعات المتاحة للتصويت.
- التصويت بصفته عضواً.

### 5. رحلة غسان
- تسجيل الدخول بحساب `ghassan@usr.edu.ye`.
- التحقق من دوره وإكمال أي خطوة مخصصة (سكرتير/عضو/مطلع).

### 6. تنظيف وإغلاق
- حذف بيانات الاختبار الموسومة `TEST_ONLY_COUNCILS_E2E_02` (اجتماعات، موضوعات، أجندة، محاضر) باستخدام RPCs الآمنة.
- تحديث `release stamp` بالقرار النهائي.
- تشغيل smoke test خفيف على الواجهة المنشورة.

## القيود الملتزم بها
- لا تعديل مباشر على `main`; جميع التعديلات عبر فرع و PR و CI.
- migration واحدة في كل مرحلة مع preflight/post-verifier.
- لا حذف أو تعديل للسجلات المحمية المذكورة في التصريح.
- لا إنشاء حسابات أو تعيينات وهمية.

## معايير النجاح
- الموضوع ينتقل فعلياً إلى `accepted_for_agenda` ويظهر في «موضوعات متاحة».
- رمزي يكمل دورة الاجتماع كاملة.
- مقبل ويحيى وغسان يكملون رحلاتهم دون أعطال.
- SHA المنشور يبقى مطابقاً للـ SHA المختبر.
