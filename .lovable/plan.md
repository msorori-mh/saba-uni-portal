الهدف: تغيير المسمى المعروض "محاضرة مساعد" إلى "محاضر مساعد" في صفحة هيئة التدريس، وإصلاح خطأ hydration ناتج عن استخدام `useQuery` + `isLoading` في العرض الأولي (SSR).

الملف المستهدف: `src/routes/faculty.tsx`.

1. تغيير المسمى المعروض:
   - في تعريف `RANK_SECTIONS` (key: `lecturer_assistant`): تغيير `title` من "محاضرة مساعد" إلى "محاضر مساعد".
   - في تعريف `OTHERS_SECTION`: تغيير `title` من "محاضرة مساعد" إلى "محاضر مساعد".
   - تغيير العناوين الفرعية `subtitle` إلى الصيغة المذكرة حسب السياق.
   - الاحتفاظ بقوائم الـ `ranks` المستخدمة للمطابقة كما هي (تحتوي على الصيغتين) لضمان استمرار تصنيف أعضاء الهيئة المسجلين بأي من الصيغتين في القسم الصحيح.

2. إصلاح خطأ hydration:
   - الاستبدال: `useQuery(facultyQuery)` → `useSuspenseQuery(facultyQuery)` (مستورد من `@tanstack/react-query`).
   - إزالة فرع `isLoading` وعناصر `Skeleton` من JSX؛ لأن البيانات محمّلة مسبقاً في الـ loader عبر `ensureQueryData`، ولا حاجة لحالة التحميل.
   - الاحتفاظ بفرع `filtered.length === 0` لعرض حالة "لا يوجد أعضاء مطابقون للبحث".
   - التأكد من أن `FacultyPage` لا يستخدم `useEffect` أو `typeof window` أو قيم متغيرة بين الخادم والعميل.

3. ملاحظات:
   - لا تغييرات على الجداول أو العلاقات أو RLS.
   - لا تغيير في منطق البحث أو الترتيب أو النافذة المنبثقة لعرض التفاصيل.
   - سيُحل خطأ hydration الحالي المرتبط باختلاف هيكل الشبكة (Skeleton grid vs. space-y-16) عند العرض الأولي.