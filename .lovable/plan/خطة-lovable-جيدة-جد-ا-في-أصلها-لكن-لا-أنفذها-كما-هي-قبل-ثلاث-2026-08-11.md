# خطة Lovable جيدة جدًا في أصلها، لكن **لا أنفذها كما هي قبل ثلاثة تعديلات مهمة**؛ واحد منها حاسم جدًا.

### 1. Policy Pinning: أوافق، لكن الـBackfill يجب ألا يُسمّى «السياسة التاريخية»

المشاريع القديمة لم تكن مثبتة على Policy أصلًا؛ كانت تقرأ السياسة الفعالة وقت كل عملية. لذلك عند الـCutover نستطيع فقط أن نقول:

```text
BACKFILL_POLICY_SOURCE = CURRENT_EFFECTIVE_POLICY_AT_CUTOVER

```

وليس الادعاء أن هذه هي السياسة التي بدأ عليها المشروع تاريخيًا.

أعتمد إضافة:

```text
policy_id
policy_snapshot
policy_pinned_at
policy_pin_source

```

مع جعل `policy_id` و`policy_snapshot` **غير قابلين للتغيير بعد التثبيت**. ويجب ألا يُسمح بتثبيت Draft؛ فقط `published`.

والـRuntime بعد ذلك يقرأ `policy_snapshot` **المثبت على المشروع**، لا السياسة المنشورة الحالية. هذا يجعل المشروع مستقلًا تمامًا عن أي V2/V3 مستقبلية.

---

### 2. المرحلة الثانية صحيحة، لكن لا يجوز لـLovable نشر «سياسة أساس» بأرقام من عنده

هذه النقطة مهمة جدًا.

القيم الحالية:

```text
فريق 1–5
نجاح 60
مشرف 1
تقرير تقدم 1
لجنة 2–5
جولتا تعديل

```

هي Defaults تقنية وليست قرارًا أكاديميًا موثقًا.

إذن الترتيب الصحيح:

```text
إنشاء Draft Baseline Policy
        ↓
عرض القيم علينا
        ↓
اعتماد أكاديمي صريح
        ↓
Publish
        ↓
إزالة fallback
        ↓
Fail Closed

```

**لا تنشر Lovable Baseline تلقائيًا.**

في هذه المرحلة تحديدًا نحتاج أن نقرر نحن القيم الأكاديمية الرسمية.

---

### 3. `allow_co_supervisor` و`max_supervisors` لا يجوز تفعيلهما الآن بهذه البساطة

هذه أكبر ملاحظة على الخطة.

النظام الحالي مبني على قاعدة:

```text
Exactly one pending/accepted supervisor

```

أما السماح بمشرف مشارك فيفتح أسئلة جديدة:

```text
من يعتمد تقرير التقدم؟
هل يكفي أحد المشرفين أم كلاهما؟
من يعلن final ready؟
هل أحدهما Primary؟
هل كلاهما يستطيع return؟
ماذا يحدث عند اختلاف القرار؟
كيف تحتسب الصلاحية؟

```

فمجرد تعديل `assign supervisor` للسماح باثنين **غير كافٍ وقد يكسر Domain Kernel**.

لذلك أعتمد الآن:

```text
allow_co_supervisor = false
max_supervisors = 1

```

ونعتبر:

```text
CO_SUPERVISOR = DEFERRED

```

حتى نصمم له عقدًا مستقلًا إذا احتجناه مستقبلًا.

---

## بقية Phase 3 أعتمدها مع تحديد دقيق


| Policy                      | الإنفاذ الصحيح                                                                |
| --------------------------- | ----------------------------------------------------------------------------- |
| `min_team_size`             | عند **Submit Proposal**، مع بقاء max عند إضافة عضو                            |
| `proposal_window_*`         | عند Submit وResubmit Proposal                                                 |
| `required_progress_reports` | يجب أن يكون العدد المقصود **تقارير معتمدة Approved**، وليس مجرد submitted     |
| `min_committee_members`     | موجود حاليًا عند إنهاء النتيجة                                                |
| `max_committee_members`     | موجود عند إضافة عضو لجنة                                                      |
| `passing_score`             | موجود عند `passed`                                                            |
| `max_revision_rounds`       | موجود                                                                         |
| `defense_window_*`          | عند Schedule Defense ويجب فحص **موعد المناقشة المحدد** لا تاريخ تنفيذ RPC فقط |


وبالنسبة إلى الفترات، يجب تحديد timezone/academic date contract بوضوح حتى لا ينتج اختلاف يوم بين UTC واليمن.

---

# نقطة إضافية مهمة: Versioning للـPolicy نفسها

الوضع الحالي فيه:

```text
published
→ superseded

```

لكن يجب التأكد أن **صف Published نفسه لا يمكن تعديله**.

أي تعديل يجب أن يكون:

```text
V1 published
        ↓
Create V2 draft
        ↓
Publish V2
        ↓
V1 superseded

```

وليس:

```text
UPDATE V1

```

ونفس الأمر للـsnapshot المثبت على المشروع.

---

# الخطة المعدلة التي أعتمدها

```text
GP-1A
Policy pinning columns + immutable contract

GP-1B
Pin all NEW projects at creation

GP-1C
Backfill existing projects using:
CURRENT_EFFECTIVE_POLICY_AT_CUTOVER
+ explicit audit evidence

GP-1D
Replace runtime dynamic policy reads
with gp_project_policy(project_id)

GP-2A
Prepare baseline policy DRAFT only

GP-2B
Academic owner approves actual values

GP-2C
Publish baseline

GP-2D
Remove built-in fallback
→ FAIL CLOSED

GP-3A
min_team_size enforcement

GP-3B
proposal submission/resubmission window

GP-3C
required APPROVED progress reports

GP-3D
defense scheduling window

GP-3E
co-supervisor remains disabled/deferred

GP-4
Parity + authorization + lifecycle regression

GP-5
Production E2E

```

## شرط الإغلاق

أريد التقرير النهائي بهذه الصورة:

```text
GP_POLICY_PINNING=PASS
EXISTING_PROJECTS_PINNED=100%
NEW_PROJECTS_PIN_AT_CREATE=PASS
POLICY_SNAPSHOT_IMMUTABLE=PASS
PUBLISHED_POLICY_IMMUTABLE=PASS

DYNAMIC_POLICY_RUNTIME_READS=0
BUILTIN_ACADEMIC_DEFAULTS=0
NO_POLICY_FAILS_CLOSED=PASS

MIN_TEAM_SIZE_ENFORCED=PASS
PROPOSAL_WINDOW_ENFORCED=PASS
REQUIRED_APPROVED_PROGRESS_ENFORCED=PASS
DEFENSE_WINDOW_ENFORCED=PASS

CO_SUPERVISOR=DEFERRED
AUTHORIZATION_REGRESSION=PASS
FULL_GP_TESTS=PASS

```

**القرار:** نعتمد خطة Lovable بعد هذه التعديلات، والأهم ألا تسمح له بنشر أرقام أكاديمية تلقائيًا ولا بتفعيل تعدد المشرفين الآن. بعد Policy Pinning مباشرة نراجع معًا قيم **سياسة مشاريع التخرج الرسمية** قبل نشرها. تثبيت سياسات مشاريع التخرج (GP) وإكمال الإنفاذ

نتيجة التدقيق مؤكَّدة من المصدر وقاعدة الإنتاج:

- جدول `graduation_projects` لا يحتوي أي عمود `policy_id` أو `policy_version` (أعمدته: القسم، البرنامج، العام، الفصل، الحالة، القرار، المتوسط، جولة التقييم…).
- دوال دورة الحياة تستدعي `gp_effective_policy(department_id, academic_year_id)` وقت التنفيذ، أي أنها تقرأ «المنشور الآن».
- `gp_effective_policy` ترجع، عند غياب أي سياسة منشورة، قيمًا مخترعة داخل الدالة: فريق 1–5، درجة نجاح 60، مشرف واحد، تقرير تقدم واحد، لجنة 2–5، جولتا تعديل.
- الحقول التالية غير مُنفَّذة في Runtime: `min_team_size`، `required_progress_reports`، `proposal_window_*`، `defense_window_*`، `allow_co_supervisor`، `max_supervisors`.

لن نعيد بناء ما هو قائم. المطلوب ثلاث إصلاحات جذرية بالترتيب.

## المرحلة 1 — تثبيت السياسة على المشروع (Policy Pinning)

القاعدة: المشروع الذي بدأ على V1 يكمل على V1؛ نشر V2 يسري على المشاريع الجديدة فقط.

- إضافة `policy_id` (مرجع إلى `graduation_project_policies`) و`policy_snapshot` (JSONB للقيم الفعلية وقت البدء) إلى `graduation_projects`.
- تثبيت السياسة عند إنشاء المشروع فقط، ولا تتغير بعد ذلك.
- استبدال كل استدعاء Runtime لـ`gp_effective_policy(...)` بقراءة السياسة المثبَّتة على المشروع، عبر دالة واحدة `gp_project_policy(project_id)`.
- المشاريع القائمة (Backfill): تثبيت السياسة المنشورة الحالية المطابقة لنطاقها الآن، مرة واحدة، دون تغيير أي سلوك حالي — وتُسجَّل كـ Backfill صريح.
- عرض «السياسة المطبَّقة على هذا المشروع» (الإصدار + النطاق) داخل مساحة المشروع للمنسق والمشرف.

## المرحلة 2 — إلغاء القرار الأكاديمي المخترع (Fail-Closed)

- إنشاء ونشر سياسة أساس واحدة صريحة (نطاق: كل الأقسام / كل الأعوام) بقيم يعتمدها الجانب الأكاديمي، بدل الاعتماد على القيم المدمجة.
- بعد وجود سياسة منشورة فعليًا: إزالة الفرع الافتراضي من `gp_effective_policy`، فتفشل العملية برسالة عربية واضحة «لا توجد سياسة مشاريع تخرج منشورة لهذا النطاق» بدل تطبيق حد مخترع.
- الترتيب إلزامي: نشر الأساس أولًا، ثم Fail-Closed، حتى لا تتعطل مشاريع قائمة.

## المرحلة 3 — إكمال الإنفاذ للحقول المعطلة

كل قاعدة تُنفَّذ في الـBackend (RPC) وتُعكس في الواجهة كرسالة، لا كحارس أمني:


| الحقل                                     | نقطة الإنفاذ                    |
| ----------------------------------------- | ------------------------------- |
| `min_team_size`                           | عند إرسال المقترح               |
| `proposal_window_start/end`               | عند إرسال المقترح               |
| `required_progress_reports`               | قبل السماح بتسليم الملف النهائي |
| `allow_co_supervisor` / `max_supervisors` | عند إسناد المشرف                |
| `defense_window_start/end`                | عند جدولة المناقشة              |


النواة الثابتة (أهلية المستوى الرابع، فريق واحد لكل طالب، فصل الأدوار، صلاحيات RPC) لا تُمس ولا تصبح قابلة للتهيئة.

## شؤون الخريجين (GA) — خارج هذه الجولة

`graduate_followups` يخزّن `purpose_code` نصًا حرًا و`state` كـ enum ثابت (open/in_progress/completed/cancelled)، أي أن دورة المتابعة ليست Configurable. يُعالَج في جولة مستقلة بعد إغلاق GP، حفاظًا على فصل النظامين.

## تفاصيل تقنية

- Migrations متتابعة، واحدة في كل مرحلة، Forward-only، مع تحقق قبلي وبعدي.
- لا تعديل على أي Migration مطبَّقة، ولا حذف بيانات، ولا تغيير على `enrollment_certificate` أو خدمات B1.
- الملفات المتأثرة في المصدر: دوال دورة حياة GP في `supabase/migrations/*`، و`src/lib/graduation-projects/*`، و`src/components/graduation-projects/*`.
- اختبارات: توسيع `tests/graduation-projects/policies.test.ts` بحالات التثبيت (نشر V2 لا يغيّر مشروعًا قائمًا)، وحالات Fail-Closed، وحالة إيجابية/سلبية لكل حقل من حقول المرحلة 3.