# MISSION:

PORTAL-MOBILE-BIOMETRIC-STEP-UP-01-PRODUCTION-APPLY

AUTHORIZATION:

APPROVED_PRODUCTION_APPLY_MOBILE_STEP_UP_01

AUTHORIZED_SOURCE_SHA:

e123bc9115921107e12f69ae96a2de40a82d4840

SCOPE:

Apply exactly ONE production migration through the Lovable-managed Supabase

for the existing production project, then perform READ-ONLY post-apply verification.

1. Verify repository/source SHA is exactly:

   e123bc9115921107e12f69ae96a2de40a82d4840

2. Verify production migration history:

   - current latest applied version = 20260813222046

   - PORTAL-MOBILE-BIOMETRIC-STEP-UP-01 has not already been applied

   - no partial objects from this migration exist with conflicting contracts

3. Promote exactly:

   docs/migration-drafts/PORTAL-MOBILE-BIOMETRIC-STEP-UP-01.sql

   into ONE correctly timestamped file under:

   supabase/migrations/

   The promoted migration must be semantically identical to the approved draft.

   No unrelated fixes, cleanup, refactors, grants, RLS changes, or migration edits.

4. Apply this ONE migration to the Lovable-managed production Supabase.

5. Immediately after apply, STOP all production writes.

6. Perform READ-ONLY post-apply verification only:

   A. Migration history

   - new migration appears exactly once.

   B. Tables

   - public.student_trusted_devices

   - public.step_up_challenges

   - public.step_up_proofs

   - RLS enabled as specified.

   C. Function signatures / contracts

   - register_student_device(text,text,text,text)

   - issue_step_up_challenge(text,text,uuid,text)

   - mint_step_up_proof(uuid)

   - consume_step_up_proof(text,text,uuid,text)

   - revoke_student_device(text)

   - revoke_all_student_devices()

   - submit_b1_student_request_atomic_core(uuid,text,jsonb,timestamptz,uuid[])

   - submit_b1_student_request_atomic 5-arg wrapper

   - submit_b1_student_request_atomic 7-arg protected overload

   D. Actual privilege verification using both:

   - pg_proc.proacl

   - has_function_privilege(...)

   Required:

   register_student_device:

     PUBLIC=DENY

     anon=DENY

     authenticated=DENY

     service_role=ALLOW

   issue_step_up_challenge:

     PUBLIC=DENY

     anon=DENY

     authenticated=DENY

     service_role=ALLOW

   mint_step_up_proof:

     authenticated=DENY

     service_role=ALLOW

   consume_step_up_proof:

     authenticated=DENY

   submit_b1_student_request_atomic_core:

     PUBLIC=DENY

     anon=DENY

     authenticated=DENY

   revoke_student_device:

     authenticated=ALLOW

   revoke_all_student_devices:

     authenticated=ALLOW

7. READ-ONLY regression checks:

   - enrollment_certificate objects/data remain unchanged

   - protected student request/document records remain unchanged

   - no unrelated migration was applied

   - no user/test request was created

   - no E2E mutation occurred

STRICTLY FORBIDDEN:

- Deploy

- Publish

- APK/AAB build

- E2E production writes

- test data insertion

- student request creation

- cleanup/reset/rollback on failure

- modifying previously applied migrations

- applying any second migration

ON ANY PREFLIGHT OR APPLY FAILURE:

STOP IMMEDIATELY.

Do not reset.

Do not cleanup.

Do not delete objects.

Do not attempt another migration.

FINAL OUTPUT:

If successful:

PASS_PRODUCTION_APPLY_MOBILE_STEP_UP_01

READY_FOR_ANDROID_PHYSICAL_RETEST=YES

Include:

- source SHA

- promoted migration filename

- applied migration version

- production apply result

- table/RLS inventory

- function signature inventory

- exact proacl snapshot

- has_function_privilege matrix

- enrollment_certificate regression verdict

- migration count applied in this mission = 1

- PRODUCTION_WRITE = MIGRATION_ONLY

- DEPLOY = 0

- PUBLISH = 0

- E2E_WRITE = 0

- TEST_DATA_WRITE = 0

If anything fails:

HOLD_PRODUCTION_APPLY_MOBILE_STEP_UP_01_<EXACT_BLOCKER>تطبيق إنتاجي واحد: PORTAL-MOBILE-BIOMETRIC-STEP-UP-01

النطاق: ترقية مسودة الترحيل الوحيدة إلى الإنتاج، ثم فحص قراءة فقط. لا Deploy، لا Publish، لا كتابة بيانات، لا E2E.

## ما سيتم تنفيذه

1. ترقية المسودة `docs/migration-drafts/PORTAL-MOBILE-BIOMETRIC-STEP-UP-01.sql` كما هي دلاليًا إلى ترحيل إنتاجي واحد في `supabase/migrations/`.
2. تطبيق هذا الترحيل على الإنتاج (Migration واحدة فقط في هذه المرحلة).
3. Post-Apply قراءة فقط للتحقق.

## محتوى الترحيل (كما هو مثبت في المصدر)

- جداول: `student_trusted_devices`، `step_up_challenges`، `step_up_proofs` مع GRANTs وRLS.
- دوال الثقة: `register_student_device`، `issue_step_up_challenge`، `mint_step_up_proof`، `consume_step_up_proof` — ممنوحة لـ`service_role` فقط.
- دوال الإبطال: `revoke_student_device`، `revoke_all_student_devices` — تبقى لـ`authenticated`.
- إغلاق مسار التجاوز: `submit_b1_student_request_atomic_core` غير ممنوحة للعميل، وwrapper 5-args محمي، وتوقيع 7-args يتطلب إثباتًا صالحًا لمرة واحدة.

## Preflight قبل التطبيق

- التأكد أن SHA المصدر يطابق `e123bc91...`.
- التأكد أن آخر إدخال في سجل الترحيلات هو `20260813222046` وعدم وجود تطبيق جزئي سابق لهذه المسودة.
- التأكد من عدم وجود كائنات تحمل الأسماء نفسها بعقد مختلف.

## Post-Apply (قراءة فقط)

- سجل الترحيلات يحتوي الإصدار الجديد.
- وجود الجداول الثلاثة وتفعيل RLS وسياساتها.
- توقيعات الدوال كما هي محددة أعلاه.
- `proacl` وhas_function_privilege يثبتان:
  - `register_student_device`: PUBLIC/anon/authenticated = DENY، service_role = ALLOW
  - `issue_step_up_challenge`: PUBLIC/anon/authenticated = DENY، service_role = ALLOW
  - `revoke_student_device` و`revoke_all_student_devices`: authenticated = ALLOW
  - `submit_b1_student_request_atomic_core`: authenticated = DENY
- عدم تأثر `enrollment_certificate` والسجلات المحمية.

## خارج النطاق

- لا Deploy ولا Publish ولا بناء APK في هذه المرحلة.
- لا إنشاء طلبات اختبارية ولا أي كتابة بيانات.
- لا تعديل ترحيلات مطبقة سابقًا.

## القرار المتوقع

عند نجاح كل الفحوصات:

```text
PASS_PRODUCTION_APPLY_MOBILE_STEP_UP_01
READY_FOR_ANDROID_PHYSICAL_RETEST=YES
```

وعند أي فشل أو تطبيق جزئي: توقف فوري بـ `HOLD_PRODUCTION_APPLY_MOBILE_STEP_UP_01_<BLOCKER>` دون reset أو cleanup أو حذف.