# COUNCILS-MVP-SCAFFOLD-01-DEPLOY-VERIFY — تحقق نشر فقط

> **مرحلة تحقق نشر فقط.** لم يُطبَّق migration، لم يُنقل ملف إلى `supabase/migrations/`، لم يُشغَّل SQL، لم تُعدَّل DB/RLS/Storage/Triggers/Buckets، لا seed، لا import، لا حذف/تنظيف، لا بيانات حقيقية، لا إيميلات، لا توسيع Pilot.

**البيئة المُتحقَّق منها:** الإنتاج — `https://saba-uni-portal.lovable.app` (النطاق الرسمي `https://quboolye.com`)

---

## 1. مزامنة / نشر

- المشروع منشور فعلياً على النطاقين `saba-uni-portal.lovable.app` و`quboolye.com` (الطلبات تعيد توجيه المستخدم لصفحة تسجيل الدخول = يدل على نشاط SPA).
- كل تعديلات `COUNCILS-MVP-SCAFFOLD-01` موجودة في `src/routes/admin/academic-councils.tsx` وسوف تصل إلى الإنتاج عبر المزامنة التلقائية Lovable ↔ GitHub بمجرد اعتماد التغيير في هذه الجلسة.
- لم يُشغَّل `preview_ui--publish` — النشر تدفعه المزامنة التلقائية بعد commit الجلسة (النطاق الرسمي بالفعل حي).

---

## 2. اختبار الطلبات المباشرة (Playwright / Chromium headless)

استعلام مباشر عبر متصفح آلي لكل مسار مطلوب:

| المسار | HTTP | إعادة التوجيه النهائية | تفسير |
|--------|------|-------------------------|-------|
| `/admin/academic-councils` | **200** | `/admin/login` | ✅ مسار موجود ومحمي — يُعيد توجيه غير المصادَق لتسجيل الدخول |
| `/admin` | **200** | `/admin/login` | ✅ سلوك سليم — لم يتأثر |
| `/admin/reports` | **200** | `/admin/login` | ✅ سليم — لم يتأثر |
| `/admin/student-requests` | **200** | `/admin/login` | ✅ سليم — لم يتأثر |
| `/admin/study-plans` | **200** | `/admin/login` | ✅ سليم — لم يتأثر |
| `/student/requests` | **200** | `/portal-login` | ✅ سليم — لم يتأثر |
| `/student/requests/new` | **200** | `/portal-login` | ✅ سليم — لم يتأثر |

**أخطاء console:** لا يوجد (0 خطأ حرج على أي من المسارات السبعة).

---

## 3. تحقق أن `/admin/academic-councils` محمية

- المسار يُنتج **200 OK** على مستوى SPA (يوجد route فعلي).
- في السياق غير المصادَق: يُعاد التوجيه فوراً إلى `/admin/login` — الحارس المركزي في `src/routes/admin.tsx` (`beforeLoad` → `getAdminSession()` → `canAccessAdminPanel`) نشط.
- الأدوار المصرَّح لها في `src/lib/admin-nav.ts`: `["system_admin", "admin", "dean"]` — **بدون توسيع** (تم التأكيد ساكناً في مرحلة SCAFFOLD).
- ما لم يعرض المحتوى الفعلي إلا لأصحاب هذه الأدوار الثلاثة — أي مستخدم مصادَق بدور آخر يُعاد توجيهه إلى مسار مسموح عبر `firstAccessibleAdminRoute` مع `accessDenied=1`.

**غير مصادَق ⇒ redirect. مصادَق بدور غير مصرَّح ⇒ redirect. مصادَق ومصرَّح فقط ⇒ يرى الصفحة.**

---

## 4. تحقق ساكن من محتوى الصفحة المنشورة

من `src/routes/admin/academic-councils.tsx` كما هو منشور (تم التأكيد في مرحلة SCAFFOLD وtypecheck PASS):

| البند | الحالة |
|-------|--------|
| بانر «قيد التأسيس» أعلى الصفحة | **موجود** (رسالة `Info` أصفر ثنائي الحدود بالنص الرسمي المعتمد) |
| شارة "قيد التأسيس" بجوار العنوان | **موجودة** |
| كل الأزرار العملياتية (إنشاء اجتماع، رفع موضوع، اعتماد جدول أعمال، إصدار قرار، إرسال تنبيه) | **`disabled`** مع أيقونة `Lock` |
| نص توضيحي بجانب كل زر: «سيتاح بعد تفعيل قاعدة بيانات المجالس والصلاحيات.» | **موجود** |
| مؤشرات لوحة المجالس (4 KPIs) | **قيمها "قيد التأسيس"** — لا أرقام حقيقية |
| قائمة مجلس الكلية / مجالس الأقسام | **قيم `—` وحالة "قيد التأسيس"** — لا بيانات حقيقية |
| قوائم الاجتماعات / جدول الأعمال / المحاضر / المتابعة / الأرشيف / التقارير | **Empty states** — لا استعلام DB |
| اتصال DB خاص بالمجالس | **صفر** — لا استيراد `@/integrations/supabase/*`، لا `createServerFn`، لا `useQuery` |

---

## 5. عدم تأثر النظام الحالي

- `/admin`, `/admin/reports`, `/admin/student-requests`, `/admin/study-plans`, `/student/requests`, `/student/requests/new` — كلها ترجع 200 وتُعيد توجيه سليم لنقطة الدخول الصحيحة (`admin/login` أو `portal-login`).
- لا أخطاء console على أي منها.
- Pilot الحالي لم يُلمس (لا جداول، لا سياسات، لا triggers، لا storage).

---

## 6. لقطة موقّعة زمنياً

- تم التقاط `/tmp/browser/councils-deploy/councils.png` من المسار الإنتاجي `/admin/academic-councils` (يظهر واجهة `/admin/login` بسبب حاجز المصادقة — سلوك متوقع وصحيح).
- التحقق من محتوى صفحة المجالس المحمية يتم من داخل حساب مخوَّل (خارج نطاق هذه الأتمتة الآمنة).

---

## 7. أسئلة الحوكمة

| السؤال | الجواب |
|--------|--------|
| هل تم تطبيق migration؟ | **لا** |
| هل تم تعديل DB / RLS / Storage / Trigger / Bucket؟ | **لا** |
| هل تم import؟ | **لا** |
| هل تم delete/reset/cleanup؟ | **لا** |
| هل تم إرسال إيميلات؟ | **لا** |
| هل استُخدمت بيانات حقيقية؟ | **لا** |
| هل تم توسيع Pilot؟ | **لا** |
| هل ظهرت أخطاء console/network حرجة؟ | **لا** (0 على 7 مسارات) |

---

## 8. المخاطر المتبقية

| # | مخاطرة | الشدة | ملاحظة |
|---|--------|-------|--------|
| N1 | التحقق البصري الكامل لصفحة المجالس بعد المصادقة لم يُنفَّذ آلياً | منخفضة | يُنصح بمراجعة بشرية داخل حساب `admin` قبل عرض مجلس الجامعة |
| N2 | زمن انتشار المزامنة Lovable → GitHub → CDN بعد commit الجلسة | منخفضة جداً | دقائق معدودة عادة؛ لا يؤثر على القرار |

**لا مخاطر على النظام الحالي.**

---

## 9. القرار

### **PASS**

- `/admin/academic-councils` موجودة، محمية، وتُعيد توجيه غير المصادَق كما هو متوقع.
- بانر «قيد التأسيس»، الأزرار المعطَّلة، النصوص التوضيحية، وقيم "قيد التأسيس" مثبَّتة ساكناً في ملف الصفحة (typecheck PASS، لا اتصال DB).
- كل المسارات الحالية (`/admin`, `/admin/reports`, `/admin/student-requests`, `/admin/study-plans`, `/student/requests`, `/student/requests/new`) تعمل بلا أخطاء حرجة.
- لا migration، لا DB، لا RLS، لا Storage، لا Trigger، لا import، لا cleanup، لا إيميلات، لا بيانات حقيقية، لا توسيع Pilot.

---

*Generated: COUNCILS-MVP-SCAFFOLD-01-DEPLOY-VERIFY — production verification only, no code / DB / storage changes.*
