# ADMIN-DASHBOARD-CARDS-RENAME-HIDE-01 Report

**التاريخ:** 2026-07-07  
**المستودع:** `C:\projects\saba-uni-portal-git`  
**القرار:** **PASS_WITH_NOTES**

---

## 1. Executive Summary

تم تعديل واجهة لوحة الأدمن فقط: إعادة تسمية «تسجيل الطلاب» إلى «تقسيم المجموعات»، وإخفاء بطاقة/روابط المرشحين للتخرج والخصومات. لم يُعثر على بطاقة بعنوان «الترفيعات» في الكود الحالي — أُضيفت إلى قائمة الإخفاء الوقائية على لوحة التحكم الرئيسية.

---

## 2. الملفات المعدلة

| الملف | التغيير |
|-------|---------|
| `src/components/admin/AdminShell.tsx` | إعادة تسمية + إزالة رابط «مرشحو التخرج» من القائمة الجانبية |
| `src/routes/admin/index.lazy.tsx` | فلتر بطاقات مخفية على `/admin` |
| `src/routes/admin/academic-operations.tsx` | إعادة تسمية رابط سريع |
| `src/routes/admin/enrollments.tsx` | إعادة تسمية عنوان الصفحة |
| `src/routes/admin/finance.lazy.tsx` | إخفاء تبويب «الخصومات والإعفاءات» |

---

## 3. إعادة التسمية: تسجيل الطلاب → تقسيم المجموعات

| الموقع | قبل | بعد |
|--------|-----|-----|
| القائمة الجانبية (`AdminShell`) | تسجيل الطلاب | **تقسيم المجموعات** |
| مركز العمليات الأكاديمية — رابط سريع | تسجيل الطلاب | **تقسيم المجموعات** |
| صفحة `/admin/enrollments` — العنوان | تسجيل الطلاب في المجموعات الدراسية | **تقسيم المجموعات** |

---

## 4. إخفاء البطاقات الثلاث

### المرشحين للتخرج

| الموقع | الإجراء |
|--------|---------|
| `/admin` — قسم «التقدم الأكاديمي» | إزالة بطاقة **مرشحو التخرج** |
| القائمة الجانبية | إزالة رابط `/admin/graduation-candidates` |
| المسار `/admin/graduation-candidates` | **باقٍ** — غير محذوف |

### الخصومات

| الموقع | الإجراء |
|--------|---------|
| `/admin/finance` | إخفاء تبويب **الخصومات والإعفاءات** (أقرب عنصر UI باسم الخصومات) |
| دوال الخصومات / `DiscountsTab` | **باقية** في الملف — غير مستدعاة من الواجهة |

### الترفيعات

| الموقع | الإجراء |
|--------|---------|
| بحث المشروع | **لا يوجد** نص «الترفيعات» كبطاقة أو عنوان |
| `/admin` | أُضيفت إلى `HIDDEN_ADMIN_DASHBOARD_CARD_LABELS` للإخفاء التلقائي إن وُجدت لاحقاً |

---

## 5. ما لم يُمس (حسب النطاق)

- لا migrations ولا DB changes
- لا حذف routes أو server functions
- لا تعديل RLS/صلاحيات
- لا commit / push / PR
- مسار `/admin/graduation-candidates` ودوال المالية للخصومات ما زالت موجودة

---

## 6. الفحوصات

| الفحص | النتيجة |
|-------|---------|
| `npm run build` | ✅ نجح (exit 0) |
| `git diff --check` | ✅ بدون أخطاء whitespace |
| `git status --short` | تعديلات UI ضمن الملفات أعلاه + ملفات مراحل سابقة غير مُلتزَمة |

---

## 7. PASS_WITH_NOTES

- **الترفيعات:** لم تُوجد بطاقة بهذا الاسم في لوحة الأدمن الحالية؛ الإخفاء الوقائي مُفعّل عبر القائمة في `index.lazy.tsx`.
- **الخصومات:** أُخفيت من تبويبات الشؤون المالية وليس من لوحة `/admin` الرئيسية (لا بطاقة باسم «الخصومات» هناك).
- وصف رابط «الشؤون المالية» في العمليات الأكاديمية ما زال يذكر الخصومات نصياً — خارج نطاق البطاقات الثلاث.
