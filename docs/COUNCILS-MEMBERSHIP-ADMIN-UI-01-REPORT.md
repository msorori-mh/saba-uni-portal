# COUNCILS-MEMBERSHIP-ADMIN-UI-01 — واجهة إدارة عضويات المجالس

> **واجهة فقط.** لا migrations، لا DB/RLS/Storage، لا seed/import، لا DELETE للعضويات، لا service role في المتصفح.

---

## 1. القرار النهائي

### **PASS**

---

## 2. الملفات المعدَّلة

| الملف | التغيير |
|-------|---------|
| `src/routes/admin/academic-councils.tsx` | تفعيل قسم إدارة العضويات (عرض، بحث، ربط، تعطيل) |
| `docs/COUNCILS-MEMBERSHIP-ADMIN-UI-01-REPORT.md` | هذا التقرير |

**لم تُعدَّل** دوال الخادم في هذه المرحلة (تُستخدم كما هي من `COUNCILS-MEMBERSHIP-WRITE-FUNCTIONS-01`).

---

## 3. ملخص الواجهة

داخل `/admin/academic-councils`:

1. **اختيار المجلس** — النقر على مجلس في قسمي «مجلس الكلية» أو «مجالس الأقسام» يحدّد المجلس النشط (تمييز بصري).
2. **قسم «إدارة عضويات المجلس»** — يظهر عند الاختيار ويتضمن:
   - جدول العضويات (حالية + سابقة)
   - نموذج بحث وربط عضو
   - تعطيل العضويات الفعّالة مع تأكيد
3. **باقي الصفحة** — KPIs، اجتماعات، موضوعات، قرارات… ما زالت **قراءة فقط** مع أزرار معطّلة.
4. **شارات الحالة** — «عضويات مفعّلة» + «باقي الوظائف — قراءة فقط».

---

## 4. الدوال المربوطة بالواجهة

| الدالة | الاستخدام في UI |
|--------|-----------------|
| `getCouncilMemberships` | `useQuery` عند اختيار مجلس |
| `searchAcademicsForCouncilLink` | `useQuery` عند ≥ حرفين في حقل البحث |
| `linkAcademicToCouncil` | زر «حفظ الربط» |
| `deactivateCouncilMembership` | زر «تعطيل» + `AlertDialog` تأكيد |
| `getCouncilsSummary` | قائمة المجالس (موجود مسبقاً) |

**ثوابت مستخدمة:** `COUNCIL_LINK_MEMBER_ROLES` لقائمة الأدوار في `Select`.

---

## 5. البحث عن الأكاديميين

- حقل `Input` مع أيقونة بحث، RTL.
- الاستعلام يُفعَّل فقط عند **حرفين فأكثر** (`enabled: trimmedSearch.length >= 2`).
- حالات: تحميل (`Loader2`)، خطأ عام، «لا توجد نتائج»، قائمة نتائج قابلة للنقر.
- كل نتيجة تعرض: الاسم، البريد، الرقم الأكاديمي — بدون حقول حساسة إضافية.
- تنبيه بصري إذا كان المرشح **عضواً فعّالاً** في المجلس المختار.

---

## 6. اختيار الدور والربط

- بعد اختيار أكاديمي من النتائج يظهر `Select` بالأدوار:
  - `chair` → رئيس المجلس
  - `secretary` → أمين السر
  - `member` → عضو
  - `viewer` → مطّلع
- زر **«حفظ الربط»** يستدعي `linkAcademicToCouncil` مع `councilId`, `facultyProfileId`, `role`.
- نجاح → `toast.success` + إعادة تحميل العضويات والملخص.

---

## 7. منع التكرار

| الطبقة | السلوك |
|--------|--------|
| **الخادم** | يرفض عضوية فعّالة مكررة (مصدر الأمان الأساسي) |
| **الواجهة** | `activeUserIds` من القائمة الحالية — تعطيل زر الحفظ + تنبيه «عضو فعّال مسبقاً» في نتائج البحث |
| **رسالة الخادم** | «يوجد عضوية فعّالة لهذا العضو في المجلس بالفعل» تُعرض عبر `toast.error` دون إخفاء |

---

## 8. التعطيل بدون حذف

- زر **«تعطيل»** يظهر للعضويات **الفعّالة فقط**.
- **لا** زر حذف ولا استدعاء DELETE.
- `AlertDialog` بالنص المطلوب قبل التنفيذ.
- يستدعي `deactivateCouncilMembership`.
- نجاح → `toast.success` + `invalidateQueries` للعضويات والملخص.

---

## 9. ملاحظة RLS ودور `dean`

دالة `mapMembershipError` تحوّل أخطاء RLS/الصلاحيات إلى:

> «تعذر تنفيذ العملية بسبب قيود الصلاحيات الحالية. قد تتطلب هذه العملية تفعيل صلاحيات إضافية من قاعدة البيانات.»

- **لا** تعديل RLS ولا migration ولا service role.
- العميد (`dean`) قد يرى الواجهة لكن **قد تفشل** الربط/التعطيل/أحياناً القراءة حتى `COUNCILS-RLS-DEAN-MEMBERSHIP-01`.
- `admin` / `system_admin` يعملان عبر `is_council_admin` في RLS.

---

## 10. نتائج التحقق

| الفحص | النتيجة |
|-------|---------|
| ESLint / IDE lints | لم يُكتمل (timeout) — لا أخطاء ظاهرة يدوياً |
| typecheck (`tsgo`) | **لم يُشغَّل** — غير متوفر محلياً |
| `bun run build` | **لم يُشغَّل** — `node_modules` غير موجودة |
| migrations جديدة | **لا** |
| DB / RLS changes | **لا** |
| service role في المتصفح | **لا** |
| DELETE للعضويات | **لا** |

---

## 11. تأكيدات عدم التوسع

| البند | الحالة |
|-------|--------|
| migrations | **لا** |
| DB schema changes | **لا** |
| RLS changes | **لا** |
| Storage | **لا** |
| Email | **لا** |
| Cron | **لا** |
| seed / import | **لا** |
| service role في المتصفح | **لا** |
| حذف عضويات | **لا** |
| إنشاء أكاديمي/حساب جديد | **لا** |

---

## 12. عوائق / ملاحظات

1. **RLS لـ dean** — موثّق أعلاه؛ يُوصى بمعالجته قبل الاعتماد التشغيلي الكامل للعميد.
2. **البناء المحلي** — يحتاج `bun install` في CI/Lovable للتحقق النهائي.
3. **اختيار المجلس** — بالنقر على بطاقة المجلس؛ لا يوجد dropdown منفصل (القوائم أعلاه كافية للـ MVP).

---

## 13. التوصية التالية

### **READY_FOR_MEMBERSHIP_UI_DEPLOY_VERIFY**

- نشر التغييرات والتحقق على `/admin/academic-councils` في الإنتاج.
- اختبار ربط/تعطيل بحساب `admin` أو `system_admin`.
- توثيق سلوك `dean` عند الفشل المتوقع حتى مرحلة RLS.

---

*Generated: COUNCILS-MEMBERSHIP-ADMIN-UI-01 — UI only, no DB changes.*
