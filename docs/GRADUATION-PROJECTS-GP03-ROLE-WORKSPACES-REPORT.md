# GRADUATION-PROJECTS — GP-03 ROLE WORKSPACES REPORT

- Phase: GP-03
- Date: 2026-07-30
- Branch: `k3/graduation-projects-completion`
- Base SHA: `9b19a71c9c2e2bb84781a2ab9c2db226de2be709` (GP-02 commit)
- Migrations created: 0 — Migrations applied: 0 — Production operations: 0
- Decision: `PASS_GRADUATION_PROJECTS_GP03_ROLE_WORKSPACES_COMPLETE`

---

## 1. What was done

### 1.1 Integrated the reviewed PR #226 portal content (adopted, verified locally)

Merged `origin/feat/graduation-projects-portal-integration-01` (incl. the PR #231
visual/accessibility QA merge) into this branch and verified locally instead of
relying on its failed remote CI (infrastructure HOLD per GP-01):

- Routes: `student.graduation-project.{tsx,index.tsx,$projectId.tsx}`,
  `faculty-portal.graduation-projects.{tsx,index.tsx,$projectId.tsx}`,
  `admin/graduation-projects.{tsx,index.tsx,$projectId.tsx}`.
- `GraduationProjectPortalWorkspace.tsx` (role-driven workspace),
  `PortalRuntimeStates.tsx` (loading/empty/error/unavailable), `gp-datetime.ts`.
- Server functions: `src/lib/graduation-projects/portal.functions.ts` (21
  `createServerFn`, all behind `requireSupabaseAuth`, zod-strict inputs, no
  client-supplied actor ids, server-built object keys).
- `availability.ts` (fail-closed runtime probe; explicit local-only mock),
  `portal-privacy.ts` (student-only redaction, evaluation visibility).
- Nav entries: `AdminShell` (`/admin/graduation-projects`, roles
  `department_head`/`dean` — UI gate only), faculty dashboard card, student
  services card.
- 2 test suites: `graduation-projects-portal-integration-01.test.ts` (411 lines),
  `graduation-projects-visual-ux-qa-01.test.ts` (382 lines).

### 1.2 k3 adjustments on top of the merge

- `routeTree.gen.ts` regenerated by the official build tooling (the PR's copy
  predated B1 routes on main); the semantic-hash pin in
  `tanstack-register-stable-augmentation-01.test.ts` was updated to the
  regenerated tree (`97fec36c…`).
- `origin/main` synced (`65a17382`) — picks up the B1 CRLF test fix; no
  conflicts with GP scope.
- `portal-privacy.ts`: `co_supervisor` classified as staff (mirrors the GP-02
  SQL read contract); regression test added.
- Working-tree EOL renormalization for `.gitattributes`-covered B1 fixtures
  (no content change; fixes pre-existing CRLF environment failures).

### 1.3 Conflict resolutions

- `rpc.ts`: PR #226 duplicated 3 of the 6 wrappers added in GP-02 — deduplicated,
  single copy kept per method.
- `routeTree.gen.ts` + tanstack hash test: resolved by regeneration (tooling
  output kept verbatim, then pinned).

## 2. Role entry points (verified)

| Role | Entry point | Workspace |
|---|---|---|
| طالب | `/student/graduation-project` (+ `$projectId`) | قائمة مشاريعه، المقترح، الفريق، التسليمات، الملاحظات، المناقشة، التصحيحات، الحالة |
| المشرف / المشرف المشارك | `/faculty-portal/graduation-projects` | المشاريع المسندة، مراجعة المراحل، الملاحظات، الجاهزية، التصحيحات |
| عضو اللجنة | `/faculty-portal/graduation-projects/$projectId` | النسخة المسموح بها، تقييمه الشخصي فقط (viewer-scoped) |
| رئيس القسم | `/admin/graduation-projects` | مشاريع القسم، الاعتماد، التعيين، اللجان، التقارير |
| العميد | `/admin/graduation-projects` | القراءة الإدارية واعتماد النتائج/الأرشفة ضمن العقد |

Guards: parent layouts (`/student`, `/faculty-portal`, `/admin`) redirect
unauthenticated users via `beforeLoad`; every server function re-authenticates;
RPCs enforce direct assignment. RTL + responsive + loading/empty/error states
ship with the kit (PR #231 QA).

## 3. Test results

| Suite | Result |
|---|---|
| `bun test tests/graduation-projects` | 104 pass / 0 fail (919 expects) |
| `bun test` (full suite) | 2359 pass / 1 fail |
| `bunx tsc --noEmit` | clean |
| `bun run build` | success (routeTree + register validated) |
| `git diff --check` | clean |

The single full-suite failure is `G4 — Arabic PDF spike on Wrangler Worker
runtime` (60s timeout; no wrangler runtime in this environment). Pre-existing
and out of scope — verified unrelated: same failure exists without GP changes;
the two B1 CRLF failures seen earlier are fixed by `origin/main` sync +
working-tree renormalization (144/144 B1 preflight tests now pass).

## 4. Shared files touched (necessity documented)

| File | Reason | Diff discipline |
|---|---|---|
| `src/routeTree.gen.ts` | new GP routes | regenerated by tooling only |
| `src/lib/admin-nav.ts` | GP nav entry (+2 lines, from PR) | additive only |
| `src/components/admin/AdminShell.tsx` | GP nav item (+1 line, from PR) | additive only |
| `src/routes/student.index.tsx`, `src/routes/faculty-portal.index.tsx` | GP entry cards (from PR) | additive only |
| `tests/student-requests/tanstack-register-stable-augmentation-01.test.ts` | route-tree hash pin after regeneration | pin value only |

No B1 logic modified; no `student_visible` changes; no migrations applied.

## 5. Remaining gaps (owned by later phases)

- Server functions for team add / faculty assignment / milestone set /
  evaluation finalize → GP-04.
- Storage/upload/notification wiring → GP-05.
- Settings/rubric administration → GP-06.

## 6. Blockers

None.
